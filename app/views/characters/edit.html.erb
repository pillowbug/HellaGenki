<h1>Edit <%= @character.name %></h1>
<h2><%= @show.title %></h2>

<%= simple_form_for [@show, @character] do |c| %>
  <%= c.input :name %>
  <%= c.input :description %>
  <%= c.submit %>
<% end %>

<%= link_to "show page", show_path(@show.id) %>
<%= link_to "go home", show_characters_path %>
